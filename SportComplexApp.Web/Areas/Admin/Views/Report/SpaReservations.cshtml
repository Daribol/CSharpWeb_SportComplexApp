@using SportComplexApp.Web.ViewModels.Report
@model IEnumerable<SpaReportViewModel>

@{
    ViewData["Title"] = "Spa Services Report";
}

<div class="d-flex align-items-center justify-content-between mb-4 print-hide">
    <h2 class="mb-0" style="color:#00ffc8;">
        <i class="bi bi-flower3 me-2"></i>Spa Services Revenue
    </h2>
    <div class="d-flex gap-2">
        <button onclick="window.print()" class="btn btn-outline-neon">
            <i class="bi bi-printer me-1"></i> Print / PDF
        </button>
        <a asp-action="ExportSpaReservationsCsv" class="btn btn-neon">
            <i class="bi bi-filetype-csv me-1"></i> Export CSV
        </a>
    </div>
</div>

<div class="card card-dark border-0 shadow-sm">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-dark table-hover table-borderless align-middle mb-0 text-center">
                <thead style="border-bottom: 2px solid #00ffc8;">
                    <tr>
                        <th scope="col" class="text-start ps-4 py-3">Spa Service Name</th>
                        <th scope="col" class="py-3">Total Reservations</th>
                        <th scope="col" class="text-end pe-4 py-3">Total Revenue (BGN)</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="3" class="text-center py-5 text-muted">
                                No spa reservation data available.
                            </td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var stat in Model)
                        {
                            <tr>
                                <td class="text-start ps-4 py-3 fw-bold text-light">@stat.SpaServiceName</td>
                                <td class="py-3 text-info">@stat.TotalReservations <i class="bi bi-ticket-perforated ms-1"></i></td>
                                <td class="text-end pe-4 py-3 text-success fw-bold">
                                    @stat.TotalRevenue.ToString("F2") лв.
                                </td>
                            </tr>
                        }
                        <tr style="border-top: 1px solid rgba(255,255,255,0.1);">
                            <td class="text-start ps-4 py-3 text-neon fw-bold">TOTAL:</td>
                            <td class="py-3 fw-bold text-info">@Model.Sum(x => x.TotalReservations)</td>
                            <td class="text-end pe-4 py-3 text-success fw-bold display-6 fs-4">
                                @Model.Sum(x => x.TotalRevenue).ToString("F2") лв.
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts {
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <style>
        @@media print {
            body { background: white !important; color: black !important; }
            .print-hide, header, footer, nav, .sidebar { display: none !important; }
            .card-dark, .table-dark { background: white !important; color: black !important; }
            .text-neon, .text-success, .text-warning, .text-info { color: black !important; }
            table { border-collapse: collapse; width: 100%; }
            th, td { border: 1px solid #ddd !important; }
        }
    </style>
}